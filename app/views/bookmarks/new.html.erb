<h1>Add a new bookmark for <%= @list.name %></h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.association :movie %>
  <%= f.input :comment %>

  <%= f.submit %>
<% end %>

<%= link_to "Back", @list, class: "btn btn-primary" %>
